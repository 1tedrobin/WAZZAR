# URL of the WAZZAR backend (wazzar-backend/backend), e.g. from `npm run dev`
# there. Copy this file to .env.local and adjust if the backend isn't on the
# default port.
VITE_API_URL=http://localhost:3000
