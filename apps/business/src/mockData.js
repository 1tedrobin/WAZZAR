/* ========================================================================
   Formerly: MOCK DATA for the dashboard surfaces the WAZZAR backend had
   no model for yet. As of 2026-08-22 all four (Customers, Scheduled,
   Staff, Billing) are wired to real backend data — see api.js and
   WIRING_NOTES.md. Nothing in this app imports this file anymore.

   Left in place (empty) rather than deleted outright, in case a future
   pass adds a new screen that genuinely has no backend yet and wants
   the same "clearly-labeled placeholder data" pattern this file used to
   provide — see WIRING_NOTES.md's history for what that looked like
   (DemoBanner + an obviously-fake, single-merchant-scoped array).
   ======================================================================== */
